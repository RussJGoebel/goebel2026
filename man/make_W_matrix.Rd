% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SAR_helpers.R
\name{make_W_matrix}
\alias{make_W_matrix}
\title{Build a row-normalised spatial adjacency matrix (W) from an sf grid}
\usage{
make_W_matrix(target_grid, zero.policy = TRUE)
}
\arguments{
\item{target_grid}{An sf POLYGON object representing the target grid.}

\item{zero.policy}{Logical passed to \code{spdep::nb2mat}. If TRUE, allows
regions with no neighbours (returns zero row). Default TRUE.}
}
\value{
A sparse \code{Matrix::Matrix} of class \code{dgCMatrix} with
nrow = ncol = nrow(target_grid). Row sums are 1 for all non-isolated cells.
}
\description{
Constructs a sparse row-normalised adjacency matrix using queen contiguity
(i.e. shared edges or corners) via \code{spdep::poly2nb}. Suitable for
any polygon grid, regular or irregular.
}
\examples{
\dontrun{
W <- make_W_matrix(target_grid)
}

}
