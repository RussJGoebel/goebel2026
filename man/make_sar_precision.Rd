% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SAR_helpers.R
\name{make_sar_precision}
\alias{make_sar_precision}
\title{Build a SAR precision matrix Q from an sf grid}
\usage{
make_sar_precision(target_grid, rho = 1, zero.policy = TRUE)
}
\arguments{
\item{target_grid}{An sf POLYGON object representing the target grid.}

\item{rho}{Spatial autoregression parameter in (0, 1]. Default 1 (intrinsic prior).}

\item{zero.policy}{Logical passed to \code{spdep::nb2mat}. Default TRUE.}
}
\value{
A sparse symmetric \code{Matrix::Matrix} of class \code{dsCMatrix}
with nrow = ncol = nrow(target_grid).
}
\description{
Constructs the simultaneous autoregressive (SAR) precision matrix
\eqn{Q = (I - \rho W)^\top (I - \rho W)}, where \eqn{W} is the
row-normalised queen adjacency matrix of the grid. When \eqn{\rho = 1}
this yields an intrinsic SAR prior.
}
\details{
The scaling parameter \eqn{\tau} is not included here — it is handled
via the \code{phi} argument in \code{fastblm::fit_fastblm} and
\code{fastblm::tune_cv}.
}
\examples{
\dontrun{
Q <- make_sar_precision(target_grid)
Q <- make_sar_precision(target_grid, rho = 0.9)
}

}
