% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{rename_and_fill_proportions}
\alias{rename_and_fill_proportions}
\title{Rename proportion_* columns from numeric suffixes to land-cover names and fill NAs with 0}
\usage{
rename_and_fill_proportions(
  df,
  key,
  prefix = "proportion_",
  keep_prefix = TRUE
)
}
\arguments{
\item{df}{A data.frame/tibble with wide columns like \code{proportion_1}, ..., \code{proportion_8}.}

\item{key}{Named character vector mapping codes -> labels (names must be "1","2",...).}

\item{prefix}{String prefix used by the wide columns (default "proportion_").}

\item{keep_prefix}{If TRUE (default), keep the prefix in the new names
(e.g., "proportion_urban"); if FALSE, use just the label (e.g., "urban").}
}
\value{
\code{df} with those columns renamed and any NAs in them replaced by 0.
}
\description{
Rename proportion_* columns from numeric suffixes to land-cover names and fill NAs with 0
}
\examples{
landcover_key <- c(
  "1"="urban","2"="croplands","3"="forest","4"="shrub & scrub",
  "5"="grass","6"="water","7"="wetlands","8"="bare surface"
)
# df2 <- rename_and_fill_proportions(df, landcover_key)
}
